from math import floor
import string

class ListMaster():
    def __init__(self):
        return

    def expand(self):
        print ('in here')

    def shorten(self, web, request):
        ''' Normally we would just use an existing library hashlib is a good one.
            But we are going to roll our own using base62 and base10
        '''

        print ('in here')


    def base62(salt, x = 62):
        base = string.digits + string.lowercase + string.uppercase

        return base

    def base10(salt, x = 62):
        base = string.digits + string.lowercase + string.uppercase

        return base
